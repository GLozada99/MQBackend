from setuptools import setup, find_packages

setup(
    name="MQBACKEND",
    version="1.0.0",
    description="Backend and DB for Mobile Quoter App",
    packages=find_packages()  # __init__.py folders search
)
